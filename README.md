# Wilson's Maze

This implements maze generation using Wilson's algorithm.

See https://en.wikipedia.org/wiki/Maze_generation_algorithm#Wilson's_algorithm

## Running

This uses Vite to make auto-refresh easy.
Otherwise it's just Vanilla js.

Run with `npm run dev`.
